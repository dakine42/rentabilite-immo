{} {
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app"
}
```

Puis **Ctrl+S**, ferme et tape :
```
git add vercel.json
git commit -m "config vercel"
git push
